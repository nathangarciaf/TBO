#include "dijkstra.h"

int main(){
    Stack *pilha_operandos = init_stack();
    Stack *pilha_operadores = init_stack();
    char c;
    while(scanf("%c", &c) == 1){
        printf("%c\n",c);
    }
    return 0;
}